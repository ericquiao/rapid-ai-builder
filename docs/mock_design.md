# Mock Design

## Purpose
Define the simulated system behavior before real integrations are implemented.

## Define
- user workflow
- mocked services
- mocked integrations
- expected system behavior

## Example mocked services
- `mock_ocr_service`
- `mock_extraction_service`
- `mock_calendar_service`

These mocks must follow the same interface as the real services planned for MVP.
